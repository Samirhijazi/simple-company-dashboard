server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router uses client-side routing, so any unmatched path
    # (e.g. a refresh on /dashboard) should fall back to index.html.
    location / {
        try_files $uri /index.html;
    }
}
